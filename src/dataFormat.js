export const formataData = (data,ultimaData) =>{
    // let ultima = new Date(ultimaData)
    //  let dates = []

    // for(let i=0; i < data.length; i++){
    //     console.log(data[i])
    //     // let dataFomated = new Date(Math.abs(data[i] - ultima.getTime()))
    //     // dates.push(dataFomated)
        
    // }
    // console.log(dates)
    // console.log(ultima.getTime())
    console.log(data)
    console.log(ultimaData)
}